apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: cloud-deployments-web
  name: cloud-deployments-ingress
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-production"
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
    nginx.ingress.kubernetes.io/backend-protocol: "FCGI"
    nginx.ingress.kubernetes.io/fastcgi-index: "index.php"
    nginx.ingress.kubernetes.io/fastcgi-params-configmap: "nginx-configmap"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - cloud-deployments.dev
      secretName: cloud-deployments-tls
  rules:
    - host: cloud-deployments.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: cloud-deployments-service
                port:
                  number: 9000
