apiVersion: v1
kind: Service
metadata:
  namespace: cloud-deployments-web
  name: redis-service
  labels:
    app: redis
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
  selector:
    app: redis
